[project]
name = "kcc-api"
version = "0.1.0"
description = "Make calls to the KCC API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bs4>=0.0.2",
    "requests>=2.32.3",
    "rich>=13.9.4",
]
